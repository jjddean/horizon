backend:
  name: git-gateway
  branch: main # This must match your GitHub repo's main branch name
publish_mode: editorial_workflow
media_folder: "public/uploads" # Where media files will be stored in your repo
public_folder: "/uploads" # How media files are accessed on your deployed site

collections:
  - name: "pages"
    label: "Pages"
    files:
      - label: "Home"
        name: "home"
        file: "content/home.md" # Netlify CMS will create/edit this file